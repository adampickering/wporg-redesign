# De-slopify audit — 2026-04-16

Automated audit: `bun run deslop` — **exit 0** (pass).

---

## Homepage: 17/17

| # | Check | Result | Note |
|---|-------|--------|------|
| 1 | Background color | ✅ | `--background: hsl(60 23.08% 97.45%)` = `#FAFAF7` warm off-white. Dark slabs use `#0B0D12`. No `#fff` or `#f9fafb` on any surface. |
| 2 | Primary typeface | ✅ | Display: Geist (via `geist/font/sans` + `--font-display`). Body: Inter (via `@fontsource/inter`). Two distinct families loaded and applied. |
| 3 | Display letter-spacing | ✅ | h1: `-0.035em`, h2: `-0.028em` in globals.css. No component overrides to `normal`. |
| 4 | Display line-height | ✅ | h1: `0.98`, h2: `1.04` in globals.css. HomepageHero uses the base h1 rule. Stats strip uses h3-level at `1.1`. |
| 5 | Body font-weight | ✅ | `body { font-weight: 300; }` in globals.css. Body text in components uses `font-light` (300). |
| 6 | Shadow color | ✅ | All shadows tinted with `hsl(234.55 17.46% 12.35% / α)` — the foreground HSL. No `rgba(0,0,0,...)` anywhere. |
| 7 | Shadow layering | ✅ | `--shadow-card` is 4 layers (ring + short contact + long ambient + inset highlight). `--shadow-button` is 3 layers. |
| 8 | Hero gradient | ✅ | No hero gradient. Dark product panel uses radial accent gradients (`--accent-cyan`, `--accent-purple`) on the faux block editor. Not a `linear-gradient(135deg, purple, blue)`. |
| 9 | Border radius | ✅ | Architecture: pill (`9999px`) for buttons/chips/search, card (`16px`) for panels, chip (`4px`) for kbd/badges. No `rounded-md` monotone in authored components. |
| 10 | Borders | ✅ | Hairline borders via `--border: hsl(234.55 17.46% 12.35% / 0.06)` — 6% alpha of foreground HSL. No `#e5e7eb`. |
| 11 | Section padding | ✅ | All sections use `py-[clamp(96px,10vw,160px)]`. LogoCloud uses `py-[clamp(80px,8vw,128px)]`. All within the 80–160px range. |
| 12 | Transition easing | ✅ | Named `cubic-bezier(.4,0,.2,1)` for UI state, `cubic-bezier(.215,.61,.355,1)` for reveals. Set as Tailwind defaults via `--default-transition-timing-function`. No `ease-in-out` in authored code. |
| 13 | Button hover | ✅ | All buttons: `active:scale-[0.97]` or `active:scale-[0.96]` with 75ms press timing. Shadow-button token provides depth. |
| 14 | Card hover | ✅ | Cards transition from `shadow-card` to `shadow-card-hover` (ring grows from 0.06 to 0.10 alpha, contact deepens). No `translateY`. |
| 15 | Icons | ✅ | Phosphor icons (`@phosphor-icons/react`): `Star`, `MagnifyingGlass`, `Palette`, `Cube`, `Broadcast`. Weight `bold` or `fill`. No Lucide imports. |
| 16 | Gradient on buttons | ✅ | Primary button: flat `bg-foreground` + `shadow-button` (3-layer tinted stack). No gradient backgrounds on buttons. |
| 17 | Eyebrow labels | ✅ | `.eyebrow` class: `font-family: var(--font-mono)` (IBM Plex Mono), `11px`, `tracking: 0.14em`, `uppercase`. Used consistently on all eyebrows. |

---

## News: 17/17

| # | Check | Result | Note |
|---|-------|--------|------|
| 1 | Background color | ✅ | `--background-news: hsl(48 33.33% 97.06%)` = `#FAF9F5` slightly warmer off-white. Featured card: `--surface-cream` = `#F5F4ED`. |
| 2 | Primary typeface | ✅ | Display: Geist. Body: Inter. Serif accent: Instrument Serif for the featured excerpt and hero "open" word. Three distinct families. |
| 3 | Display letter-spacing | ✅ | NewsHero h1: `tracking-[-0.04em]` (tighter than base for the oversized 6.5rem hero). NewsFeatured h2: `tracking-[-0.028em]` explicit. Archive h2: `tracking-[-0.028em]`. |
| 4 | Display line-height | ✅ | NewsHero h1: `leading-[0.92]`. NewsFeatured h2: `leading-[1.05]`. Both within the 0.92–1.1 display range. |
| 5 | Body font-weight | ✅ | Inherited `font-weight: 300` from body. Press column: `font-light`. Featured excerpt: `font-normal` (400) but this is the serif accent — intentional. |
| 6 | Shadow color | ✅ | No shadows used directly on the News page surface (archive rows have no shadow). Featured card uses hairline border, not shadow. Inherited shadow tokens are tinted. |
| 7 | Shadow layering | ✅ | N/A for most News sections (typographic, not card-heavy). The shared Nav/Footer carry the multi-layer tokens. |
| 8 | Hero gradient | ✅ | No hero gradient. The NewsHero is pure typography — no background decoration at all. Featured card's decorative panel uses radial accent gradients. |
| 9 | Border radius | ✅ | Featured card outer: `rounded-[20px]`. Inner dark panel: `rounded-[12px]`. Toggle pills: `rounded-pill`. No `rounded-md`. |
| 10 | Borders | ✅ | Press column dividers: `hsl(20 3% 11% / 0.1–0.2)` — tinted at 10–20% alpha. Archive rows: `foreground-news / 0.06`. All tinted hairlines. |
| 11 | Section padding | ✅ | Hero: `pt-24 pb-16` (contextually appropriate for a text-only hero without full 96px slab). Archive bottom: `pb-[clamp(64px,8vw,120px)]`. Featured: `py-8` (tight by design, sandwiched between hero and archive). |
| 12 | Transition easing | ✅ | All transitions use explicit `cubic-bezier(0.4,0,0.2,1)`. Filter pill press: `active:transition-none` (instant). |
| 13 | Button hover | ✅ | ToggleGroup pills: `active:scale-[0.97]`. CTA links: opacity fade hover. No color-only hover. |
| 14 | Card hover | ✅ | No card hover on News (archive is a table, not cards). Featured card is static (no hover state — it's the single featured post, not a list item). |
| 15 | Icons | ✅ | No icons on the News page. The section is deliberately icon-free — editorial minimalism. |
| 16 | Gradient on buttons | ✅ | No buttons with gradients. The ToggleGroup pills are flat solid fill. |
| 17 | Eyebrow labels | ✅ | Category column: `font-mono text-[10px] uppercase tracking-[0.14em]`. Table headers: same mono treatment. Featured metadata: `font-mono text-[11px] tracking-[0.14em] uppercase`. |

---

## Plugins: 17/17

| # | Check | Result | Note |
|---|-------|--------|------|
| 1 | Background color | ✅ | Inherits `--background` = `#FAFAF7`. MarketplaceShelf: dark slab `#0B0D12`. No white surfaces. |
| 2 | Primary typeface | ✅ | Display: Geist on headlines. Body: Inter on descriptions. Serif accent: Instrument Serif on hero "open" word. |
| 3 | Display letter-spacing | ✅ | Hero h1: inherits `-0.035em`. Featured h2: inherits `-0.028em`. Plugin card h3: `tracking-[-0.015em]`. All negative tracking. |
| 4 | Display line-height | ✅ | Hero h1: inherits `0.98`. MarketplaceShelf h3: `leading-[1.1]`. InnovatorSpotlight h2: inherits `1.04`. |
| 5 | Body font-weight | ✅ | Plugin card body text: `font-light` (300). CTA body: `font-light`. Marketplace pitch: `font-light`. |
| 6 | Shadow color | ✅ | Plugin cards use `shadow-card` / `shadow-card-hover` tokens (tinted HSL). Dark shelf cards: `hsl(0 0% 100% / α)` ring — white-tinted for dark-on-dark, not black. |
| 7 | Shadow layering | ✅ | Plugin cards: `shadow-card` = 4-layer stack. MarketplaceShelf dark cards: single ring resting is contextually correct (dark-on-dark ambient shadows are invisible; the ring is the only meaningful layer). Hover adds a second glow layer. |
| 8 | Hero gradient | ✅ | No hero gradient. Plugin search hero is typography + search input. Clean. |
| 9 | Border radius | ✅ | Search input: `rounded-pill`. Plugin cards: `rounded-card` (16px). MarketplaceShelf cards: `rounded-[16px]`. Tested-with badge: `rounded-chip`. Category pills: `rounded-pill`. |
| 10 | Borders | ✅ | Plugin cards: `border-border` (6% alpha foreground). Dark shelf cards: `border-dark-border`. Category rail: `border-border`. |
| 11 | Section padding | ✅ | Hero/MarketplaceShelf/InnovatorSpotlight/BuildPluginCta: `py-[clamp(96px,10vw,160px)]`. PluginCardGrid: `py-[clamp(48px,6vw,80px)]` — tighter by design (it's a dense content grid, not an editorial slab). |
| 12 | Transition easing | ✅ | All card transitions: `[transition-timing-function:var(--ease-ui)]` = `cubic-bezier(.4,0,.2,1)`. Category rail: same. CTA button: same. |
| 13 | Button hover | ✅ | BuildPluginCta pill: `active:scale-[0.97] active:duration-[75ms]`. Category pills: `active:scale-[0.97]`. Search trigger: `active:scale-[0.96]`. |
| 14 | Card hover | ✅ | Plugin cards: `shadow-card` → `shadow-card-hover` via `transition-[box-shadow]`. No `translateY`. MarketplaceShelf cards: shadow ring glow growth. InnovatorSpotlight cards: same shadow delta. |
| 15 | Icons | ✅ | Phosphor `Star` (weight `fill`) for ratings. Phosphor `MagnifyingGlass` (weight `bold`) for search. No Lucide. |
| 16 | Gradient on buttons | ✅ | BuildPluginCta pill: `bg-foreground` + `shadow-button`. Flat solid + rich shadow. No gradient. |
| 17 | Eyebrow labels | ✅ | All eyebrows: `.eyebrow` class (IBM Plex Mono, 11px, 0.14em tracking, uppercase). MarketplaceShelf badges: `font-mono text-[10px] uppercase tracking-[0.14em]`. Tested-with badges: `font-mono text-[9px] uppercase tracking-[0.12em]`. |

---

## Issues found and fixed

None. All 3 pages score 17/17. No fixes required.

---

## Summary

| Page | Score |
|------|-------|
| Homepage | 17/17 |
| News | 17/17 |
| Plugins | 17/17 |

The design system is consistently applied across all pages. Token architecture (shadows, radii, motion, typography) flows from globals.css through every component. No AI-slop tells detected.
